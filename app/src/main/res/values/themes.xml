<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VIRUJ" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.VIRUJ" parent="Base.Theme.VIRUJ"
        />
    <style name = "RatingBar" parent="Base.Theme.VIRUJ">
        <item name = "colorControlNormal">@color/golden</item>
        <item name="colorControlActivated">@color/golden</item>
    </style>
</resources>